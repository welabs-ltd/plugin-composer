{
  "name": "plugin-composer",
  "version": "1.5.0",
  "description": "Gutenberg block(s) for the Plugin Composer plugin.",
  "private": true,
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "format": "wp-scripts format",
    "lint:js": "wp-scripts lint-js"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.0.0"
  }
}
